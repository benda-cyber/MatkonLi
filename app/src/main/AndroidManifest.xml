<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matanhassin.matkonli">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/matkonlilogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/matkonlilogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MatkonLi">
        <activity android:name=".activities.register_page"></activity>
        <activity android:name=".activities.login_page" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.activity_start_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>